[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
# keep empty for now unless shared needs stuff
serde = { version = "*", default-features = false, features = ["derive"] }
postcard = { version = "1.1", default-features = false }
